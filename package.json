{
  "name": "shipping-github",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/unit/review-policy.test.mjs",
    "check": "node --check scripts/review-threads.mjs && node --check scripts/pr-policy-gate.mjs && npm test"
  }
}
